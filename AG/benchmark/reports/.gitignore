# Regenerated by `pnpm --dir AG/benchmark benchmark:report`; the report is
# deterministic and traceable to its inputs, so a committed copy would only add a
# second, ageing answer. The directory itself is tracked through its README.
benchmark-report.json
benchmark-report.md
