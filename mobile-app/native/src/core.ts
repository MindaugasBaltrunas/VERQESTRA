// The single allowed seam between the native shell and the platform-independent
// MVC core: only this module may import `@verqestra/mobile-app`. Every other file
// under `native/src` imports from `./core` (or `../core`), so the `native -> core`
// dependency direction and its exact surface stay auditable in one place.
export {
  AgLoopReadController,
  ConnectionsController,
  GatewayHttpClient,
  initialAppState,
  presentConnections,
  presentDashboard,
  presentProjects,
  presentSessionReview,
  presentTasks,
  presentTerminal,
  ProjectsController,
  PushToTalkRecorder,
  reduceAppState,
  SecureCloudConsentStore,
  SessionReviewController,
  SpeechCaptureError,
  TerminalApplicationError,
  TerminalController,
  TerminalStreamClient,
  VoiceCaptureController,
  VoiceCaptureError,
} from "@verqestra/mobile-app";
export type {
  AgLoopConnectionViewState,
  AgLoopRuntimeComponent,
  AgLoopTaskBucket,
  AgLoopUiReadPort,
  AppEvent,
  AppState,
  BiometricAuthenticatorPort,
  BiometricUnlockOutcome,
  ConnectionsChannelViewState,
  ConnectionsViewProps,
  ConnectionsViewState,
  CredentialPort,
  DashboardQueueRow,
  DashboardViewProps,
  DashboardViewState,
  DeviceCredential,
  DeviceCryptoPort,
  DeviceKeyHandle,
  DeviceKeyPort,
  DeviceProofPort,
  GatewayPort,
  HostConnectionsReadPort,
  MobileHttpTransportPort,
  MobileIdPort,
  MobileWebSocketFactory,
  MobileWebSocketPort,
  ProjectsChannelViewState,
  ProjectsReadPort,
  ProjectsViewProps,
  ProjectsViewState,
  Provider,
  ReconnectSchedulerPort,
  SecureStoreKey,
  SecureStorePort,
  SessionAuditRow,
  SessionDiffFileRow,
  SessionDiffHunkRow,
  SessionDiffLineRow,
  SessionGateRow,
  SessionReviewAuditViewState,
  SessionReviewConnectionViewState,
  SessionReviewDiffViewState,
  SessionReviewFingerprintRow,
  SessionReviewGatesViewState,
  SessionReviewReadPort,
  SessionReviewViewProps,
  SessionReviewViewState,
  SpeechCapability,
  SpeechCaptureHandle,
  SpeechConsentPort,
  SpeechFinalResult,
  SpeechPartialResult,
  SpeechRecognitionPort,
  SpeechTranscriptionMode,
  SpeechUnavailableReason,
  TasksBucketTab,
  TasksViewProps,
  TasksViewState,
  TerminalActionButton,
  TerminalActionId,
  TerminalComposerViewState,
  TerminalConnectionViewState,
  TerminalLineRow,
  TerminalProviderOption,
  TerminalSession,
  TerminalStreamClientObserver,
  TerminalStreamControlPort,
  TerminalViewProps,
  TerminalViewState,
  TerminalVoicePrivacy,
  TerminalVoiceViewState,
  TerminalWriteAction,
  TerminalWriteGatePort,
  VoiceAvailability,
  VoiceCaptureFailureCode,
  VoiceCapturePhase,
  VoiceSubmissionTarget,
} from "@verqestra/mobile-app";
