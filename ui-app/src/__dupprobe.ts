// DELETE ME — reviewer scratch file, not product code.
// Created to check whether `tsc` flags duplicate keys in a flat `Record<string, string>` literal
// (it does not). The repo's bash allowlist has no delete command, so it could not be removed here.
// Safe to delete: nothing imports it.
export {};
