import { describe, it } from "vitest";

// DELETE THIS FILE — scratch probe from task 025, kept only because this sandbox's Bash/PowerShell
// policy allowlist has no file-delete command available to the agent. Not part of the task scope
// (ui-app/src/view/pages/BenchmarkPage.tsx, BenchmarkPage.test.tsx, ui-app/src/view/styles/dashboard.css).
describe.skip("scratch probe (delete this file)", () => {
  it("noop", () => {});
});
